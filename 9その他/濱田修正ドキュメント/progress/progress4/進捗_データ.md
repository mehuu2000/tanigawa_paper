# 進捗: データ登録・データ作成の改修

## 2026-07-04

### Solr 登録データ作成処理の修正

対象ファイル:

- `4プログラム/jalc-to-solr.ipynb`

修正目的:

- Solr に登録する著者表記と、参考文献文字列側の著者表記の不一致を減らす。
- `subtitle` がある文献で、Solr 側と参考文献文字列側のタイトル表記を揃える。

### 修正内容

#### 1. `creator` に英語著者の空白ありフルネームを追加

修正前:

```python
creator_list += last_name.split()
first_name = name.get("first_name","")
creator_list += first_name.split()
```

修正後:

```python
creator_list += last_name.split()
first_name = name.get("first_name","")
creator_list += first_name.split()
# 英語著者などの空白ありフルネームを追加
if last_name and first_name and (regex.search(first_name) and regex.search(last_name)):
    creator_list.append(last_name + " " + first_name)
```

これにより、英語著者について `last_name`、`first_name` に加えて、`last_name first_name` も `creator` に登録される。

例:

```text
Nontachai
TITHIPONGTRAKUL
Nontachai TITHIPONGTRAKUL
```

#### 2. `first_author` に英語筆頭著者の `first_name` と空白ありフルネームを追加

修正前:

```python
if last_name:
    fa_names.append(last_name)
else:
    fa_names.append(first_name)
```

修正後:

```python
if last_name:
    fa_names.append(last_name)
    if first_name and (regex.search(first_name) and regex.search(last_name)):
        fa_names.append(first_name)
        fa_names.append(last_name + " " + first_name)
else:
    fa_names.append(first_name)
```

これにより、英語筆頭著者について `last_name` だけでなく、`first_name` と `last_name first_name` も `first_author` に登録される。

例:

```text
Nontachai
TITHIPONGTRAKUL
Nontachai TITHIPONGTRAKUL
```

#### 3. `title | subtitle` を `title subtitle` に変更

修正前:

```python
title_list.append(t.get("title"))
if (subtitle := t.get("subtitle")) is not None:
    title_list.append(t.get("title") + " | " + subtitle)
```

修正後:

```python
title_list.append(t.get("title"))
if (subtitle := t.get("subtitle")) is not None:
    title_list.append(t.get("title") + " " + subtitle)
```

これにより、`subtitle` がある文献では、Solr 側にも参考文献文字列作成側と同じ `title subtitle` 表記が登録される。

`title` 単体の登録は維持している。

### 確認結果

`4プログラム/jalc-to-solr.ipynb` について、以下を確認済み。

- `creator` に `last_name + " " + first_name` を追加する処理が入っている。
- `first_author` に `first_name` と `last_name + " " + first_name` を追加する処理が入っている。
- `title + " | " + subtitle` は残っていない。
- `title + " " + subtitle` に変更されている。

### 次に行うこと

Solr の登録データを再構築する。

その後、正例データ作成側と負例データ側の修正に進む。

## 正例データ作成処理の修正

対象ファイル:

- `4プログラム/create_references_none_typo_crossref.ipynb`

対象セル:

- `Cell 17`: 評価実験用の誤植なし正例データ作成
- `Cell 21`: 予備実験用の誤植なし正例データ作成

修正内容:

- LSJ 形式の著者生成で、英語著者または非日本語著者を `last_name + first_name` ではなく `last_name + " " + first_name` に変更した。
- 日本語著者は従来通り `last_name + first_name` のまま。

追加した関数:

```python
def format_lsj_creator_name(name):
    last_name, first_name = name[0], name[1]
    if last_name and first_name and not has_japanese(last_name):
        return last_name + " " + first_name
    return last_name + first_name
```

確認結果:

- `Cell 17` と `Cell 21` の両方で `format_lsj_creator_name()` を利用していることを確認した。
- 実行はまだ行っていない。

## 負例データの直接補正

負例データは Gemini 生成タイトルとの対応関係を壊さないため、MongoDB から再生成せず、既存 LSJ 負例ファイルを直接補正した。

対象ファイル:

- `5データ/5-1予備実験用データ/5-1-2参考文献文字列（架空）/negative_reference_lsj_pre.txt`
- `5データ/5-2評価実験用データ/5-2-2参考文献文字列（架空）/negative_reference_lsj_eval.txt`

JSAI / IPSJ の負例ファイルは変更していない。

### 置換対応表

#### 予備実験用

| 行 | 置換前 | 置換後 |
|---:|---|---|
| 98 | `CLANUWATTarin` | `CLANUWAT Tarin` |
| 98 | `LAMBAlex` | `LAMB Alex` |
| 261 | `AyiguliAINI` | `Ayiguli AINI` |

#### 評価実験用

| 行 | 置換前 | 置換後 |
|---:|---|---|
| 113 | `SupasitSrivaranun` | `Supasit Srivaranun` |
| 148 | `NontachaiTITHIPONGTRAKUL` | `Nontachai TITHIPONGTRAKUL` |
| 149 | `Kang-JunLiu` | `Kang-Jun Liu` |

### 補正対象外

以下は英字を含むが、空白なしの `last_name + first_name` 連結ではないため補正対象外とした。

- `CFRP成形加工研究委員会`
- `Ngo Le Hoang Minh`
- `Weikun Zhen`
- `Yaoyu Hu`
- `Yu-Jhe Li`
- `Soji Yamakawa`
- `Sebastian Scherer`
- `Kris Kitani`

### 確認結果

- `negative_reference_lsj_pre.txt` の行数は 510 行のまま。
- `negative_reference_lsj_eval.txt` の行数は 818 行のまま。
- 行順は変更していない。
- タイトル、学会名、年は変更していない。
- LSJ 負例ファイル内に、補正対象となる空白なし英語著者候補が残っていないことを確認した。

## 負例データから Crossref 由来行を削除

現在の環境は Crossref を除いた JaLC のみのデータで MongoDB / Solr を再構築している。

しかし、負例データは Crossref が入っていた過去環境で作成されたものを継続利用していた。

そのため、負例データ側にも Crossref 由来の行が残っている可能性があった。

### 削除候補の確認

詳細な削除候補は以下に記録した。

- `9その他/濱田修正ドキュメント/progress/progress4/負例Crossref削除候補.md`

判定方法:

1. 旧 `doi_list.txt` と Crossref なし `doi_list.txt` を比較する。
2. Crossref なし DOI リストに存在しない DOI を削除対象 DOI とする。
3. 負例データの各行から Gemini 生成タイトルを抽出する。
4. `llm_create_titles.txt` または `llm_create_titles_2.txt` の行順に対応付ける。
5. 対応した旧 DOI が削除対象 DOI なら、その負例行を削除候補とする。

### 削除内容

JSAI / IPSJ / LSJ は同じループで同時生成されているため、同じ行番号を3スタイルから同時に削除した。

削除した行:

- 予備実験用負例: 1〜10行目
- 評価実験用負例: 1〜19行目

対象ファイル:

- `5データ/5-1予備実験用データ/5-1-2参考文献文字列（架空）/negative_reference_jsai_pre.txt`
- `5データ/5-1予備実験用データ/5-1-2参考文献文字列（架空）/negative_reference_ipsj_pre.txt`
- `5データ/5-1予備実験用データ/5-1-2参考文献文字列（架空）/negative_reference_lsj_pre.txt`
- `5データ/5-2評価実験用データ/5-2-2参考文献文字列（架空）/negative_reference_jsai_eval.txt`
- `5データ/5-2評価実験用データ/5-2-2参考文献文字列（架空）/negative_reference_ipsj_eval.txt`
- `5データ/5-2評価実験用データ/5-2-2参考文献文字列（架空）/negative_reference_lsj_eval.txt`

### 削除後の件数

予備実験用負例:

- `negative_reference_jsai_pre.txt`: 500件
- `negative_reference_ipsj_pre.txt`: 500件
- `negative_reference_lsj_pre.txt`: 500件

評価実験用負例:

- `negative_reference_jsai_eval.txt`: 799件
- `negative_reference_ipsj_eval.txt`: 799件
- `negative_reference_lsj_eval.txt`: 799件

### 確認結果

- 予備実験用、評価実験用ともに JSAI / IPSJ / LSJ の行数が一致している。
- 空行は 0 件。
- 削除対象 DOI に対応する Gemini 生成タイトルの残存は 0 件。
- DOI 復元できる JSAI / IPSJ 行について、Crossref 由来 DOI の残存は 0 件。
- サンプル確認でも、同じ行番号で3スタイルが同じ文献を指している。

補足:

- 予備実験用の一部行は、著者名に全角括弧やコロンを含む特殊行のため、単純なタイトル抽出では対応に失敗する。
- ただし、今回の削除対象 DOI に対応するタイトルは残っていないため、Crossref 由来行の削除漏れは確認されていない。
